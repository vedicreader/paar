# reprs


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

The rendering contract for untrusted, arbitrary objects: **never raise,
never be huge**.
[`safe_repr`](https://vedicreader.github.io/paar/reprs.html#safe_repr)
truncates and swallows broken `__repr__`s;
[`get_shape`](https://vedicreader.github.io/paar/reprs.html#get_shape)/[`get_dtype`](https://vedicreader.github.io/paar/reprs.html#get_dtype)
sniff numpy/pandas-style metadata without importing either. Anything
fancier (custom per-type strings) lives in `providers`, not here.

``` python
from paar.reprs import safe_repr, get_shape, get_dtype
class _Fake:
    def __init__(self, shape=None, dtype=None):
        if shape is not None: self.shape=shape
        if dtype is not None: self.dtype=dtype
class _Bad:
    def __repr__(self): raise ValueError('boom')

def test_safe_repr_ok():    assert safe_repr('hi')=="'hi'"
def test_safe_repr_trunc():
    r=safe_repr('a'*500, 10); assert len(r)==10 and r.endswith('…')
def test_safe_repr_bad():   assert 'repr-error' in safe_repr(_Bad())
def test_shape_attr():      assert get_shape(_Fake(shape=(3,4)))=='(3, 4)'
def test_shape_len():       assert get_shape([1,2,3])=='3'
def test_shape_str_none():  assert get_shape('hello') is None
def test_shape_scalar_none():assert get_shape(5) is None
def test_dtype_attr():      assert get_dtype(_Fake(dtype='float32'))=='float32'
def test_dtype_none():      assert get_dtype(5) is None
for t in [test_safe_repr_ok,test_safe_repr_trunc,test_safe_repr_bad,test_shape_attr,
          test_shape_len,test_shape_str_none,test_shape_scalar_none,test_dtype_attr,test_dtype_none]: t()
```

    /opt/hostedtoolcache/Python/3.12.13/x64/lib/python3.12/site-packages/usearch/__init__.py:131: UserWarning: Will download `usearch_sqlite` binary from GitHub.
      warnings.warn("Will download `usearch_sqlite` binary from GitHub.", UserWarning)

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/paar/blob/main/paar/reprs.py#L11"
target="_blank" style="float:right; font-size:smaller">source</a>

### safe_repr

``` python
def safe_repr(
    v, n:int=300
):
```

*repr(v) truncated to n chars, never raising.*

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/paar/blob/main/paar/reprs.py#L18"
target="_blank" style="float:right; font-size:smaller">source</a>

### get_shape

``` python
def get_shape(
    v
):
```

*tuple(v.shape) as a string; else len(v) for sized non-strings; else
None.*

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/paar/blob/main/paar/reprs.py#L30"
target="_blank" style="float:right; font-size:smaller">source</a>

### get_dtype

``` python
def get_dtype(
    v
):
```

*str(v.dtype) when present, else None.*
